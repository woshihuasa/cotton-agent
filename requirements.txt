# ── DeepSeek API ──
openai>=1.0.0
python-dotenv>=1.0.0
langchain-openai>=0.2.0

# ── LangChain 生态 ──
langchain>=0.3.0
langchain-community>=0.3.0
langchain-chroma>=0.2.0

# ── 向量数据库 ──
chromadb>=0.5.0

# ── 文档解析 ──
pymupdf>=1.24.0

# ── 图形界面 ──
PyQt6>=6.7.0

# ── Markdown 渲染 ──
markdown>=3.5.0

# ── 数据处理 ──
pandas>=2.0.0
matplotlib>=3.8.0

# ── Token 计算（L2 压缩）──
tiktoken>=0.7.0

# ── 原生工具（联网搜索、天气查询）──
tavily-python>=0.5.0
requests>=2.31.0

# ── 打包（可选：仅构建 exe 时需要）──
# pyinstaller>=6.0
