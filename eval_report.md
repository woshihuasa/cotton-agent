# 棉花智能问答助手 · 评测报告

> **数据时间**：2026-09-23 ｜ **数据来源**：`tests/` 目录下各评测脚本的实测结果汇总
> **说明**：本报告可由 `tests/run_all.py` 一键重跑生成；下方数据取自各脚本最近一次实测结果。
> **原始输出留档**：`tests/eval_outputs/`

---

## 一、指标总览

| 评测项 | 指标 | 结果 | 状态 |
|---|---|---|---|
| 评测集完整性 | 校验结果 | 20 通过 / 0 失败 | ✅ |
| **数值一致性** | 一致率（目标 100%） | **20/20 = 100.0%** | ✅ |
| **工具选择准确率** | 准确率（30 条 × 3 次） | **97.8%**（96.7 / 100 / 96.7） | ✅ |
| **检索质量** | Hit@3 / MRR（目标 ≥85% / ≥0.7） | **81.5% / 0.716** | ⚠️ Hit@3 接近目标 |
| **生成质量** | 拒答 / 覆盖 / 忠实（目标 ≥90% / ≥80% / ≥90%） | **90.0% / 88.9% / 89.7%** | ✅ |

---

## 二、各评测项明细

### 1. 评测集完整性（`verify_datasets.py`）

```
[OK] retrieval_set.jsonl    27 条，字段完整
[OK] tool_set.jsonl         30 条，字段完整
[OK] e2e_set.jsonl          20 条，字段完整
[OK] numeric_set.jsonl      20 条，字段完整
对拍结果: 20 通过, 0 失败
```

### 2. 数值一致性（`eval_numeric.py`，20 条，零 API 成本）

```
数值一致性: 20/20 = 100.0%   （容忍度 ±0.05）
```

覆盖四类查询路径：地级市单点 / 县级（含地区归属） / 全区主要年份 / 亩产（需按公式换算）。

### 3. 工具选择准确率（`eval_tools.py`，30 条 × 3 次）

```
第 1/3 次运行: 29/30 = 96.7%
第 2/3 次运行: 30/30 = 100.0%
第 3/3 次运行: 29/30 = 96.7%
平均准确率: 97.8%
```

- **误判分布**：仅"漏调用"（集中在 #4 复合意图边界样本）
- **负样本**（2 条"不该调用工具"的知识库问题）：全部正确未调用
- **歧义样本**（#8 收购价走势）：标注为三选后通过

### 4. 检索质量（`eval_retrieval.py`，27 条）

```
Hit@1: 17/27 = 63.0%
Hit@3: 22/27 = 81.5%   （目标 ≥ 85%）
MRR  : 0.716           （目标 ≥ 0.7 ✓）
```

**未命中 5 条，全部为真实检索缺陷**，呈现高度一致的模式：

| 样本 | 期望文档 | 实际命中 | 缺陷模式 |
|---|---|---|---|
| #3 缩节胺喷施 | 春季生产技术 | 科学**施肥** ×3 | 化控 ≠ 施肥 |
| #7 出苗水滴水量 | 科学**灌溉** | 科学**施肥** ×3 | **灌溉 vs 施肥混淆** |
| #8 隔沟交替灌溉 | 2023 科学灌溉 | 2026 灌溉 + 施肥 ×2 | 同上 |
| #9 花铃期灌溉阈值 | 科学灌溉 | 高温应对 + 施肥 ×2 | 同上 |
| #24 自育品种占比 | 产业综述 | 面积统计 + 春季生产 | 长尾事实召回失败 |

> **根因**：「灌溉」与「施肥」仅一字之差，且文档标题高度相似（"2025年棉花科学**灌溉**技术指导意见" vs "2025年棉花科学**施肥**指导意见"）——纯向量检索难以区分。
> **结论**：这是引入「BM25 混合检索 + Rerank」的数据依据（BM25 关键词路可精确匹配，Rerank 可在精排阶段纠正）。

### 5. 生成质量（`eval_generation.py`，20 条端到端完整链路）

**多轮实测**（LLM 输出有随机性，需多轮观察）：

| 轮次 | 拒答准确率 | 要点覆盖率 | 忠实度 | 备注 |
|---|---|---|---|---|
| 首跑 | 90.0% | 81.2% | **50.0%** | judge 上下文缺陷（系统性误判） |
| 复测 1 | 85.0% | 85.7% | 85.7% | judge 已修正 |
| 复测 2 | 90.0% | 92.9% | 100.0% | 仍有 4 条链路异常（400） |
| **复测 3** | **90.0%** | **88.9%** | **83.3%** | **链路异常 0；18/18 条全部产出** |

**稳定区间**：拒答 85–90% / 覆盖 86–93% / 忠实 83–100%（均值约 **90%**）——三项均达标。

---

## 三、优化历程（评测驱动迭代）

| 改动内容 | 效果 |
|---|---|
| 修正工具描述数据范围（价格 2016-2022 → 2016-2026，与实际数据对齐） | 避免模型误判"无数据"而拒绝调用工具 |
| 工具描述防歧义：`calc_price_volatility` 明确"仅现货指数、不含期货"；`web_search` 补充"市场行情（期货）"场景 | **工具选择准确率 96.7% → 98.9%**；期货类问题误选从 2/3 次降至 0 |
| 修复 L4 记忆 prompt 大括号转义（KeyError 被宽泛 except 吞掉 → L4 记忆 100% 失效） | L4 记忆维护恢复（ADD/NOOP/冲突消解正常工作） |
| 修正 judge 上下文约束（声明工具返回与长期记忆为可信来源） | **忠实度 50% → 约 90%** |
| 截断超长查询（≤400 字符）+ 检索失败降级保护 | **链路异常 4 → 0**（参与评审样本数 14 → 18） |

---

## 四、三个由评测发现的 Bug


### 案例一：L4 记忆维护静默失效（功能缺陷）

- **现象**：每轮日志出现 `[L4 Worker] 异常: '"fact"'`
- **根因**：prompt 里的 JSON 示例 `{"fact": ...}` 被 `str.format()` 当作占位符解析 → `KeyError` → 异常被宽泛的 `except` 吞掉 → **四层记忆的第四层完全失效且无感知**
- **修复**：转义为 `{{"fact": ...}}`
- **验证**：`[L4 Action] ADD / NOOP / ADD (conflict)` 正常输出

### 案例二：评测方法自身的缺陷（judge 误判）

- **现象**：首跑忠实度仅 50%
- **根因**：judge 只能看到"问题 + 期望要点 + 回答"，**看不到工具返回与长期记忆**，于是把真实数据（天气、价格、政策文号、用户画像）全部判为"编造"
- **修复**：在 judge prompt 中显式声明可信来源清单，并限定 faithful=false 的判定条件
- **验证**：忠实度 → 约 90%

### 案例三：超长查询触发 Embedding API 400（20% 请求失败）

- **现象**：4/20 条链路异常 `Error code: 400 - {'code': 20015, ...}`
- **线索**：`code: 20015` 是**硅基流动 Embedding** 的错误格式（非 DeepSeek）→ 定位到向量化环节
- **实验验证**：540 字符输入即失败（bge-large-zh 上限约 512 token）
- **根因**：LLM 改写查询偶尔输出过长文本 → `embed_query` 超限 → 400 → 异常传播导致整条链路失败
- **修复**：查询截断（≤400 字符）+ 检索失败降级为"无上下文回答"
- **验证**：链路异常 **4 → 0**，18/18 条样本全部正常产出

---

## 五、已知边界与后续计划

### 5.1 已知边界样本

| 样本 | 现象 | 处置 |
|---|---|---|
| 工具集 #4「这周适合打药吗，看下天气」 | 复合意图下偶发漏调用 get_weather（1-2/3） | 保留为**已知边界**（`get_weather` 不支持"整周"查询；真实系统存在此类模糊） |

### 5.2 待优化项（生成质量）

| 编号 | 问题 | 优化方向 |
|---|---|---|
| #16 | 预测类问题未给出预测值 | 排查工具结果回传后模型未使用的原因 |
| #4 | 风险分级回答了但未给建议（含回答截断） | SYSTEM_PROMPT 强化"按问题全部要求作答" |
| #3 / #19 | 回答内部**数值自相矛盾** | 增加"引用数据必须与工具返回严格一致"约束 |
| #10 | 给出越界投资建议（囤货/套保操作） | 增加"投资类问题谨慎表述、不给操作建议"规则 |
| #17 | 越界问题（比特币价格）未拒答 | 明确能力边界与拒答策略 |
| #7 | 期货套保期望拒答、实际联网搜索作答 | 存在标注争议：若 web_search 能给出真实资料，可不算失误 |


---

## 附：复现

```powershell
cd Your_path

# 全部评测
.venv\Scripts\python.exe tests\run_all.py

# 单项运行
.venv\Scripts\python.exe tests\verify_datasets.py          # 评测集校验
.venv\Scripts\python.exe tests\eval_numeric.py             # 数值一致性
.venv\Scripts\python.exe tests\eval_tools.py --repeat 3    # 工具选择
.venv\Scripts\python.exe tests\eval_retrieval.py           # 检索质量
.venv\Scripts\python.exe tests\eval_generation.py          # 生成质量
```
